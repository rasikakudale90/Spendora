# Spendora Backend API

Java 17 / Spring Boot 3 REST API Service for Spendora.

## Setup & Execution

```bash
# Build
mvn clean package

# Run locally
mvn spring-boot:run
```

API documentation is accessible at `http://localhost:8080/swagger-ui.html` when running.
